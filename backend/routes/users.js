const express = require('express');
const { auth } = require('../middleware/auth');

const router = express.Router();

// User profile routes are in auth.js
// This is for admin user queries

module.exports = router;
